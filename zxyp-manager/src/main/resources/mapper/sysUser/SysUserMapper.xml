<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.xccit.zxyp.mapper.SysUserMapper">

    <!--配置了自动映射,这里手动映射一次-->
    <resultMap id="sysUserVO" type="io.xccit.zxyp.entity.system.SysUser">
        <id column="id" property="id"></id>
        <result column="username" property="userName"></result>
        <result column="password" property="password"></result>
        <result column="name" property="name"></result>
        <result column="phone" property="phone"></result>
        <result column="avatar" property="avatar"></result>
        <result column="description" property="description"></result>
        <result column="status" property="status"></result>
        <result column="create_time" property="createTime"></result>
        <result column="create_time" property="createTime"></result>
        <result column="is_deleted" property="isDeleted"></result>
    </resultMap>
    
    <sql id="userVo">
        select `id`,`username`,`password`,`name`,`phone`,`avatar`,`description`,`status`,`create_time`,`update_time`,`is_deleted`
                from `sys_user`
    </sql>

    <select id="selectUserInfoByUserName" resultType="io.xccit.zxyp.entity.system.SysUser">
        <include refid="userVo"></include>
        <where>
            <if test="inputUsername != null">
                username = #{inputUsername}
            </if>
        </where>
    </select>
</mapper>